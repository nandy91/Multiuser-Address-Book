Java console application to demonstarte multi user Client Server program.The server accepts connection from multiple clients. The Server first brings all the records from the file “AddressBook.txt” into the server internal data structure(LinkedHashMap). The server performs the appropriate action depending on the commands sent by the client on the data structure and later saves the entire content into the file after command execution.